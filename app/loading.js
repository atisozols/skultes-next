const loading = () => {
  return (
    <div className="flex h-full w-full flex-1 items-center justify-center">
      <span className="loading loading-dots loading-lg text-background dark:text-foreground"></span>
    </div>
  );
};

export default loading;
